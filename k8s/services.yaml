---
apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: soc-platform
spec:
  selector:
    app: dashboard
  ports:
    - name: streamlit
      port: 8501
      targetPort: 8501
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: soc-platform
spec:
  selector:
    app: api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: soc-platform
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
  type: ClusterIP
